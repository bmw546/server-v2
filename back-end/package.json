{
  "name": "base_server_v2",
  "version": "0.0.1",
  "description": "Base server for an home (lan) server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "marc",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
